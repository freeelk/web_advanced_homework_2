.intro
    .header
        .container
            .socials
                +socials('socials-intro')
            .hamburger гамбургер
    .greeting-pages
        .greeting__photo-container
            img.greeting__photo(src="images/author.png", alt=`Фото автора: ${$.site.author}` )
        .greeting__author #{$.site.author}
        .greeting__name #{$.site.name}
